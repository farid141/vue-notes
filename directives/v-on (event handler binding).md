event handler ketika sebuah aksi terjadi pada sebuah DOM element

```html
<button v-on:click.prevent="handleClick"></button>
```

keterangan:

- `v-on:` (directive) alias `@:`
- `click` (argument) event
- `prevent`, (modifiers)
- `handleClick`, (value)
